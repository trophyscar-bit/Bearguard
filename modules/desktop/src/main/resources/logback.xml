<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

    <property name="LOG_DIR" value="${frostguard.log.dir:-log}"/>
    <property name="LOG_FILE" value="${LOG_DIR}/frostguard.log"/>
    <!-- "make everything EST in logs" — FILE_PATTERN's numeric offset (XXX,
         e.g. -04:00) was correct but not obviously a timezone at a glance, and APP_PATTERN carried
         no zone info at all. Both pinned to America/New_York (so it reads EST/EDT correctly across
         the DST boundary, not a fixed always-wrong-half-the-year offset) with the abbreviation
         spelled out (zzz) instead of a bare numeric offset. -->
    <property name="APP_PATTERN" value="%d{HH:mm:ss.SSS zzz, America/New_York} %-5level [%thread] %logger{32} - %msg%n"/>
    <property name="FILE_PATTERN" value="%d{yyyy-MM-dd'T'HH:mm:ss.SSS zzz, America/New_York} %-5level [%thread] %logger{48} - %msg%n"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.out</target>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${APP_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <append>true</append>
        <immediateFlush>true</immediateFlush>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${FILE_PATTERN}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/archive/frostguard.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>8MB</maxFileSize>
            <maxHistory>10</maxHistory>
            <totalSizeCap>80MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <logger name="dev.frostguard" level="INFO"/>
    <logger name="dev.frostguard.engine.emulator" level="DEBUG"/>
    <logger name="dev.frostguard.engine.schedule" level="DEBUG"/>

    <logger name="com.android.ddmlib" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="ERROR"/>
    <logger name="com.sun.javafx" level="ERROR"/>
    <logger name="javafx" level="ERROR"/>
    <logger name="javax.persistence" level="ERROR"/>
    <logger name="org.hibernate" level="ERROR"/>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="ROLLING_FILE"/>
    </root>
</configuration>
